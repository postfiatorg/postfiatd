# Logrotate configuration for postfiatd debug.log
# Rotates when log exceeds 100MB, keeps 5 rotated copies

/var/log/postfiatd/debug.log {
    size 100M
    rotate 5
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
    dateext
    dateformat -%Y%m%d-%H%M%S
}
